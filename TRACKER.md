# فایل پیگیری وظایف هوش مصنوعی (AI Task Tracker)

این فایل بر اساس قوانین تعیین‌شده توسط کاربر ساخته شده است تا همیشه تصویری کلی از معماری پروژه و تغییراتی که اعمال می‌شود در دسترس باشد.

## نمای کلی پروژه
- **نام پروژه:** حساب سرمایه‌گذاران + حقوق و دستمزد (hesab-app)
- **معماری:** Next.js (App Router) + Supabase/MySQL
- **بخش‌های اصلی:**
  1. **حساب سرمایه‌گذاران (`/equity`):** سیستم محاسبه سهم وزنی شمش آلومینیوم و تراکنش‌های شرکا.
  2. **حقوق و دستمزد (`/payroll`):** مدیریت کارمندان، حقوق پایه، اضافه‌کاری، ثبت تراکنش‌ها/پرداختی‌های ماهانه، گزارش چاپ ماهانه و اتصال با کیف‌پول.
  3. **هاب اصلی (`/`):** داشبورد ورود و انتخاب ماژول.
- **قوانین ثابت:**
  - رعایت دقیق طراحی راست‌چین (RTL).
  - استفاده از اعداد فارسی و کامای لاتین برای جداکننده هزارگان (`,`).
  - فونت Vazirmatn (وزن‌های 400، 600، 700 - بدون استفاده از 800).
  - هماهنگی کامل داده‌ها با دیتابیس بدون هیچ‌گونه خطا یا اختلال در بخش‌های قبلی.

---

## وضعیت وظایف (Task Status)
- [x] **قابلیت ویرایش تراکنش‌های ثبت‌شده در صفحه ماهانه کارمندان:**
  - افزودن دکمه «ویرایش» در ستون عملیات جدول تراکنش‌ها در `EmployeeClient.tsx`.
  - تبدیل ردیف به حالت اینلاین با فیلدهای تاریخ، نوع پرداخت، شرح و مبلغ ریالی.
  - تعبیه دکمه‌های «ذخیره» و «انصراف».
  - اتصال به `PATCH /api/payroll/payments` و به‌روزرسانی آنی محاسبات و استیت‌ها.
- [x] **درج جمع کل مبالغ ستون مبلغ در انتهای جدول:**
  - افزودن ردیف `<tfoot>` در انتهای جدول تراکنش‌ها.
  - نمایش جمع کل ریالی پرداختی‌های ماه (`formatRial(calculated.cPays)`) دقیقاً زیر ستون مبلغ پرداختی.
- [x] تست موفقیت‌آمیز بیلد با دستور `npm run build` بدون هیچ‌گونه خطای تایپ یا کامپایل.

---

## تاریخچه تغییرات
- **۱۴۰۴/۰۶/۲۲:** راه‌اندازی اولیه هاب و تفکیک ماژول‌های حقوق و دستمزد و حساب سرمایه‌گذاران.
- **۱۴۰۵/۰۷/۰۱:** پیاده‌سازی قابلیت ویرایش اینلاین پرداختی‌های ماهانه کارمند و درج سطر جمع کل زیر ستون مبلغ پرداختی در `EmployeeClient.tsx` با تأیید کاربر.
