# فایل پیگیری وظایف هوش مصنوعی (AI Task Tracker)

این فایل بر اساس قوانین تعیین‌شده توسط کاربر ساخته شده است تا همیشه تصویری کلی از معماری پروژه و تغییراتی که اعمال می‌شود در دسترس باشد.

## نمای کلی پروژه
- **نام پروژه:** hesab-app-main (حساب سرمایه‌گذاران + حقوق و دستمزد)
- **معماری فعلی:** Next.js (App Router) + Supabase/MySQL 
- **سیستم قدیمی:** کد درون یک فایل `standalone.html` بوده که الان درون `src/app/page.tsx` و از طریق `public/app.js` رندر می‌شود. تمامی محاسبات حساس در کلاینت (`app.js`) انجام می‌شود. احراز هویت با JWT Cookie است (`/api/auth`).
- **قوانین ثابت:**
  - رعایت دقیق طراحی راست‌چین (RTL).
  - استفاده از اعداد فارسی.
  - فونت Vazirmatn (وزن‌های 400، 600، 700 - بدون استفاده از 800).
  - عدم ایجاد اختلال (Zero-Regression) در عملکرد کنونی بخش حساب سرمایه‌گذاران (API ها، تبدیل وزن، جداکننده‌ها و...).

## وظیفه فعلی (Current Task)
**پیاده‌سازی هاب مرکزی (Hub) و تفکیک ماژول‌ها:**
- [ ] انتقال `src/app/page.tsx` کنونی به `src/app/equity/page.tsx`.
- [ ] ساخت فایل `src/app/page.tsx` جدید به عنوان داشبورد هاب با دو کارت اصلی:
  - حساب سرمایه‌گذاران (Equity)
  - حقوق و دستمزد (Payroll)
- [ ] ساخت صفحه خالی پایه برای `/payroll`.
- [ ] افزودن دکمه برگشت به خانه در ماژول سرمایه بدون خراب کردن ساختار فایل `app.js` یا `body.ts`.
- [ ] مدیریت وضعیت ورود (Login) در صفحه اصلی هاب.

## تاریخچه تغییرات
- **تاریخ ایجاد فایل:** ۱۴۰۴/۰۶/۲۲ (برابر با 2026-09-21)
- *منتظر تایید طرح پیشنهادی از سوی کاربر برای شروع کدنویسی...*
