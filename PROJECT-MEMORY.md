🧠 حافظهٔ پروژه — حساب سرمایه‌گذاران
این فایل برای دستیار هوش مصنوعی است.
در شروع هر گفتگوی جدید، این فایل را به دستیار نشان دهید تا بداند قبلاً چه گذشته
و تصمیم‌های گرفته‌شده را دوباره از صفر بررسی نکند.

آخرین به‌روزرسانی: ۱۴۰۴/۰۶/۲۲ (2025-09-13)
وضعیت: کلیدهای Supabase واقعی کاربر داخل فایل قرار گرفته‌اند ← نرم‌افزار در حالت ابری است.
پروژهٔ کاربر: gwparoiysljdbumjncui.supabase.co
سایت آنلاین است: hesab-app-nine.vercel.app
⚠️ جدول‌ها با نسخهٔ اول اسکریپت ساخته شدند (ستون occurred_at به‌جای jdate)
← خطای «column transactions.jdate does not exist».
راه‌حل: docs/fix-jdate.sql (جدول‌ها خالی بودند، بازسازی بی‌خطر بود).
RLS فعال و تأییدشده · ثبت‌نام عمومی خاموش و تأییدشده.

۱. این نرم‌افزار چه کار می‌کند؟
دو (یا چند) سرمایه‌گذار در صنعت آلومینیوم شریک‌اند.
واحد حسابداری ریال نیست — کیلوگرم شمش آلومینیوم است.

هر تراکنش پولی در لحظهٔ ثبت با قیمت همان لحظه به وزن تبدیل می‌شود:

text

weight_kg = amount_rial / price_rial_per_kg
واریز (IN)  ← وزن مثبت
برداشت (OUT) ← وزن منفی
وزن ثابت می‌ماند؛ ارزش ریالی با نوسان قیمت تغییر می‌کند.
درصد سهم هیچ‌وقت ذخیره نمی‌شود — همیشه از روی وزن‌ها محاسبه می‌شود:
share_i = weight_i / total_weight × 100

۲. تصمیم‌های قطعی‌شده با کاربر
موضوع	تصمیم
قیمت شمش	دستی، در هر تراکنش وارد می‌شود (نه API، نه جدول قیمت روزانه)
واحد پول	ریال به ازای هر کیلوگرم
کاربران	فعلاً فقط یک مدیر؛ در آینده شاید کاربر اضافه شود
تعداد شرکا	مدل داده چند-شریکی است، نه قفل‌شده روی دو نفر
ذخیره‌سازی	دو حالته: اگر window.SUPA پر باشد ← Supabase؛ وگرنه localStorage
احراز هویت	Supabase Auth با ایمیل/رمز. ثبت‌نام عمومی باید خاموش باشد
زبان	فارسی، راست‌چین، ارقام فارسی
تاریخ	فقط شمسی، ذخیره به شکل رشتهٔ YYYYMMDD (مثلاً 14050622)
۳. خواسته‌های صریح کاربر (این‌ها را تغییر ندهید مگر بگوید)
جداکنندهٔ هزارگان باید «,» باشد نه «٬» فارسی.
دلیل: کاربر گفت جداکننده باید زیر عدد بنشیند نه بالای آن. کامای لاتین روی خط پایه است.
ورود خودکار جداکننده هنگام تایپ در همهٔ فیلدهای مبلغ.
تاریخ بدون اسلش تایپ می‌شود: کاربر 14050622 می‌زند، سیستم خودش ۱۴۰۵/۰۶/۲۲ می‌کند.
عنوان نرم‌افزار «حساب سرمایه‌گذاران» است و زیرنویس ندارد. (قبلاً «سامانهٔ پایش سهم شرکا» بود، عوض شد.)
در همهٔ باکس‌های آماری، عنوان و عدد باید نسبت به هم و نسبت به کادر وسط‌چین باشند.
فونت وزیرمتن به‌صورت base64 داخل فایل جاسازی شده.
⚠️ آن را با لینک CDN جایگزین نکنید — کاربر گفت فونت‌ها واضح نبودند و علتش این بود که فونت نصب نبود.
⚠️ از وزن 800 استفاده نکنید؛ وزیرمتن آن را ندارد و مرورگر مصنوعی ضخیم می‌کند و تار می‌شود. فقط ۴۰۰/۶۰۰/۷۰۰.
CSV برای ورود داده کافی نیست — کاربر گفت فارسی به هم می‌ریزد. روش اصلی کپی/پیست و خواندن مستقیم xlsx است.
در خروجی PDF (فقط PDF، نه نمایش صفحه):
از بخش ۳ فقط کادر «مانده وزنی»، از بخش ۴ فقط کادر «مانده ریالی».
با کلاس‌های .pr-off (حذف در چاپ) و .pr-solo (وسط‌چین کردن کادر باقی‌مانده) پیاده شده.
۴. ساختار فایل‌ها
text

hesab-app/
├── public/index.html    ← کل نرم‌افزار، یک فایل مستقل (~۲۷۰ کیلوبایت)
│                         خط ~۱۲۷: بلوک window.SUPA (url + key)
├── vercel.json          ← تنظیمات استقرار استاتیک
├── README.md            ← راهنمای کاربر
├── PROJECT-MEMORY.md    ← همین فایل
└── docs/
    ├── DEPLOY.md            راهنمای گام‌به‌گام GitHub + Vercel
    ├── SUPABASE.md          راهنمای راه‌اندازی ابر و رمز عبور
    ├── fix-app-settings.sql ساخت جدول app_settings (اجرا شد)
    ├── fix-jdate.sql        بازسازی جدول‌ها با ستون jdate درست
    ├── DESIGN.md            مدل داده و فرمول‌ها
    ├── supabase-schema.sql  اسکریپت آمادهٔ Supabase (هنوز استفاده نشده)
    └── sample-577.xlsx      فایل نمونه برای تست ورود گروهی
همه‌چیز در یک فایل HTML است. بدون npm، بدون بیلد، بدون وابستگی بیرونی.
این عمدی است — کاربر فنی نیست و سادگی اولویت دارد.

۵. نقشهٔ داخلی index.html
ترتیب بخش‌های <script>:

بخش	توضیح
داده‌ها + localStorage	partners, txs, uid, curPrice, save(), load(), flash()
اعداد	toFa, digits, group, fa, kg, parseNum, bindMoney
تاریخ	bindDate, dkey, dshow, validDate, todayJ
محاسبات	price(), W(t), stats() ← قلب منطق
رندر	render, drawPie, drawLine, renderTx, renderPartners
پیش‌نمایش وزن	prevCalc, prev, eprev
عملیات	fillSel, editTx, delTx, editP, delP
گزارش	buildReport() ← چهار بخش
ورود گروهی	parseCSV, parseTSV, guessCols, normDate, normKind, unzip, inflate, sheetToRows, dx, doImport
پشتیبان	dl, صادرات JSON/CSV, بازیابی, renderStor
ساختار داده
JavaScript

partners = [{ id:'p…', name:'…', color:'#4ade80', note:'' }]
txs      = [{ id:1, p:'p…', k:'IN'|'OUT', a:5000000000, pr:1850000, d:'14050622', t:'شرح' }]
a = مبلغ ریال (همیشه مثبت) · pr = قیمت شمش · d = تاریخ شمسی رشته‌ای · t = توضیحات

۶. منطق گزارش (buildReport) — ظرافت مهم
بخش ۱ و ۲: فقط تراکنش‌های داخل بازه. ستون‌ها دقیقاً: تاریخ، مبلغ، توضیحات. (کاربر صریحاً گفت فقط این سه.)
بخش ۳: مانده وزنی از روز اول تا تاریخ پایانی — تاریخ شروع نادیده گرفته می‌شود.
این عمدی است و در گزارش هم نوشته شده تا ابهام نباشد.
بخش ۴: مانده وزنی × آخرین قیمت ثبت‌شده در بازه (از هر تراکنشی، نه فقط این شریک).
اگر در بازه هیچ تراکنشی نبود ← آخرین قیمت قبل از بازه، با هشدار قرمز.
اعداد تأییدشده در تست
سناریو: تراکنش قبل از بازه +2000kg، داخل بازه +2000, −500, +200، قیمت مبنا 4,000,000:

text

جمع واریز بازه  = 3,500,000,000 ✓
جمع برداشت بازه = 1,000,000,000 ✓
مانده وزنی      = 3,700.00 kg   ✓
مانده ریالی     = 14,800,000,000 ✓
اگر منطق گزارش را تغییر دادید، با همین سناریو دوباره تست کنید.

۷. خوانندهٔ xlsx — نکتهٔ فنی مهم
بدون هیچ کتابخانه‌ای نوشته شده: ZIP را دستی باز می‌کند و با DecompressionStream('deflate-raw') مرورگر از حالت فشرده درمی‌آورد.

⚠️ باگی که یک بار رخ داد و رفع شد: اکسل گاهی متن فارسی را به شکل &#1578; ذخیره می‌کند.
تابع dx() این را رمزگشایی می‌کند. اگر روزی متن‌ها به شکل عدد و &# دیده شدند، مشکل از همین‌جاست.
dx() باید روی هر سه جا اعمال شود: سلول‌ها، sharedStrings، و نام شیت‌ها.

۷.۵ لایهٔ ابری (Supabase) — معماری
اصل کلیدی: حالت دوگانه. بالای <body> بلوکی هست:

JavaScript

window.SUPA = { url:"", key:"" };
خالی ← CLOUD=false ← دقیقاً رفتار قبلی، localStorage، بدون صفحهٔ ورود
پر ← CLOUD=true ← صفحهٔ ورود اجباری، منبع حقیقت سرور است
⚠️ هر تغییری در CRUD دادید، باید هر دو حالت را تست کنید.

اجزا
تابع	کار
api(path,opt)	fetch با هدرهای apikey/Authorization
login/refresh/logout	Supabase Auth؛ توکن در localStorage['alu_equity_token']
cloudLoad()	خواندن partners + transactions + قیمت از سرور
push(fn,label)	صف ترتیبی نوشتن؛ خطا را به کاربر نشان می‌دهد (بی‌صدا رد نمی‌شود)
boot()	نقطهٔ شروع؛ جایگزین load() قدیمی شده
جدول‌ها
partners · transactions (تاریخ jdate char(8) شمسی، weight_kg ستون generated) · app_settings (قیمت جاری)
RLS: فقط authenticated. کاربر anon هیچ دسترسی ندارد.

⚠️ دو باگی که در مسیر رخ داد و رفع شد
شناسه‌ها در حالت محلی عدد و در ابر UUID هستند.
همهٔ مقایسه‌ها باید String(a)===String(b) باشند و در HTML حتماً onclick="editTx('${t.id}')" با کوتیشن.
بعد از ساخت شریک، id موقت با id سرور عوض می‌شود.
اگر <select> روی id قدیمی مانده باشد، تراکنش با partner خالی ذخیره می‌شود.
fillSel() حالا مقدار نامعتبر را بازنمی‌گرداند و بعد از تعویض id، انتخاب UI هم به‌روز می‌شود.
وضعیت تست (۲۲/۲۲ با سرور ماک)
ورود/رد رمز غلط · ماندگاری نشست · بارگذاری از سرور پس از پاک کردن localStorage ·
دستگاه دوم همان داده را می‌بیند · ویرایش و حذف بین دستگاه‌ها منتقل می‌شود ·
ورود گروهی ۶۰ ردیفی روی سرور · خروج · و رگرسیون کامل حالت محلی.

⚠️ درس مهم: ناسازگاری ساختار جدول
نسخهٔ اول supabase-schema.sql تاریخ را occurred_at timestamptz تعریف کرده بود،
ولی نرم‌افزار تاریخ شمسی رشته‌ای jdate char(8) می‌فرستد.
کاربر نسخهٔ اول را اجرا کرده بود ← خطای «column transactions.jdate does not exist».

اقدام‌های اصلاحی:

docs/fix-jdate.sql ساخته شد (drop + recreate).
docs/supabase-schema.sql حالا خوددرمان است: بلوک do $$ در ابتدای آن
وجود ستون occurred_at را بررسی و جدول‌های قدیمی را حذف می‌کند.
پیام خطای نرم‌افزار حالا نام فایل درست را پیشنهاد می‌دهد.
قاعده: اگر ساختار جدول را تغییر دادید، حتماً مسیر مهاجرت برای کسی که
نسخهٔ قبلی را اجرا کرده در نظر بگیرید.

۷.۷ ممیزی عمیق (۱۴۰۴/۰۶/۲۲) — دو باگ جدی رفع شد
کاربر گزارش داد «گاهی کرش می‌کند یا هیچی نشان نمی‌دهد و باید چند بار رفرش کنم».
با سرور ماک و تزریق خطا (۵۰۰/۴۰۱/تأخیر/قطع شبکه) بازتولید شد.

باگ ۱ — داشبورد خالی به‌جای پیام خطا
afterLogin() صفحهٔ ورود را پنهان می‌کرد، بعد cloudLoad() شکست می‌خورد.
نتیجه: کاربر یک داشبورد ظاهراً واردشده ولی کاملاً خالی می‌دید.
alert هم یک‌بار می‌آمد و بعد رد می‌شد ← «هیچی نشان نمی‌دهد».

رفع: پرچم LOADED + showBanner() — نوار قرمز چسبان بالای صفحه با
دکمه‌های «تلاش دوباره» و «خروج». دیگر هرگز صفحهٔ خالی بی‌توضیح دیده نمی‌شود.

باگ ۲ — گم شدن بی‌صدای داده (جدی‌تر)
در push() اگر نوشتن شکست می‌خورد فقط یک flash() گذرا نشان داده می‌شد.
کاربر تراکنش را روی صفحه می‌دید ولی روی سرور نبود؛ با اولین رفرش ناپدید می‌شد.
این دقیقاً «داده‌ها بعد از رفرش پاک می‌شوند» است.

رفع:

۳ بار تلاش مجدد با backoff (۰.۷s، ۱.۴s) — خطای گذرای شبکه خودکار جبران می‌شود
اگر باز هم شکست خورد: saveFailureNotice() — نوار قرمز ماندگار پایین صفحه،
شمارش تغییرات ذخیره‌نشده، هشدار «رفرش نکنید»، دکمهٔ «همگام‌سازی دوباره» و «پشتیبان بگیر»
beforeunload — مرورگر هنگام بستن صفحه با تغییرات ذخیره‌نشده هشدار می‌دهد
سخت‌سازی اضافی
stats() حالا isFinite(w) را بررسی می‌کند تا یک رکورد خراب کل صفحه را سفید نکند
مواردی که تست شد و سالم بود
دو کلیک سریع (تکراری نمی‌شود) · ماندگاری قیمت · حذف شریک دارای تراکنش ·
دو تب همزمان · گشت در همهٔ تب‌ها · گزارش · حالت محلی · شکست refresh ← بازگشت به ورود

نتیجهٔ نهایی: ۲۲/۲۲

⚠️ هنگام هر تغییر در لایهٔ ابری، حتماً مسیرهای شکست را هم تست کنید، نه فقط مسیر موفق.

۷.۸ یکپارچه‌سازی با کیف پول — فاز ۱ (۱۴۰۴/۰۶/۲۲)
مخزن کیف پول: github.com/AmirMazinani561/Arena_Wallet (Next.js + Drizzle، حسابداری دوطرفه).

تصمیم‌ها: واحد هر دو ریال (بدون تبدیل) · کیف پول روی Supabase جدا ·
فقط اشخاص نام‌برده شریک‌اند · fee نادیده گرفته می‌شود.

پیاده‌سازی‌شده در این نرم‌افزار:

status = pending | confirmed؛ pending از همهٔ محاسبات خارج است
(در stats() با return زودهنگام و در W() با بازگشت صفر — دو لایه محافظت)
تب «در انتظار قیمت» + نشان تعداد + هشدار داشبورد + شمارندهٔ «+N منتظر»
تأیید تکی و دسته‌ای
source / source_id با ایندکس یکتا برای ضد تکرار
docs/fix-pending.sql (قیمت nullable، ستون محاسباتی وزن بازسازی شد)

WALLET-SYNC.md
 — نگاشت کامل فیلدها و کد آمادهٔ Webhook
تست: ۱۹/۲۰ یکپارچه‌سازی (یک مورد ایراد خود تست بود، دستی تأیید شد) + ۱۳/۱۳ رگرسیون.

نکتهٔ مهم: اگر هر دو طرف تراکنش شریک باشند، منتقل نمی‌شود (خنثی از نظر سرمایهٔ کل).

باقی‌مانده: افزودن equity-sync.ts به مخزن کیف پول + تنظیم متغیرهای محیطی.

۷.۹ بازنویسی به Next.js — نسخهٔ ۲ (۱۴۰۴/۰۶/۲۲)
تصمیم کاربر: «مسیر ب» — بازنویسی کامل به Next.js با بک‌اند واقعی،
به‌جای وصله‌کاری روی فایل تک‌HTML. هدف: قفل نشدن به Vercel و Supabase.

چه چیزی عوض شد
نسخهٔ ۱	نسخهٔ ۲
معماری	فایل HTML تک، مرورگر ← Supabase	Next.js 16، مرورگر ← API ← دیتابیس
دیتابیس	فقط Supabase	MySQL / MariaDB / PostgreSQL
احراز هویت	Supabase Auth	جدول users + bcrypt + JWT در کوکی httpOnly
کلید دیتابیس	داخل HTML (قابل دیدن)	فقط روی سرور
شناسه	uuid + gen_random_uuid()	varchar(64) ساخته‌شده در برنامه
ستون محاسباتی	weight_kg generated	حذف شد (در کلاینت حساب می‌شود)
ویو	v_partner_balances	حذف شد (استفاده نمی‌شد)
فایل‌های کلیدی

client.ts
 — لایهٔ اتصال دوگانه، جای‌نگهدار ? خودکار به $1 تبدیل می‌شود

repo.ts
 — ساخت خودکار جدول‌ها روی هر دو گویش، بدون قابلیت اختصاصی

auth.ts
 — bcrypt + jose، کوکی httpOnly، seedFirstUser() از env

route.ts
 — دریافت از کیف پول با توکن مشترک
wallet-patch/ — کد آمادهٔ سمت کیف پول

standalone.html
 — نسخهٔ قدیمی نگه داشته شد
باگ‌های واقعی که در تست پیدا و رفع شدند
next/script اسکریپت را اجرا نمی‌کرد (فقط preload) ← تگ <script defer> ساده
newId تعریف نشده بود — هنگام حذف بلوک window.SUPA از دست رفت؛
افزودن شریک بی‌صدا شکست می‌خورد. ← به ابتدای app.js منتقل شد
mysql2 برای DDL آرایه برنمی‌گرداند ← Array.isArray بررسی شد
استخر اتصال با تغییر DATABASE_URL بازسازی نمی‌شد ← کلید URL ذخیره و مقایسه می‌شود
نتایج آزمون
۲۱/۲۱ روی PostgreSQL (مرورگر واقعی، Playwright)
۲۱/۲۱ روی MySQL/MariaDB — همان کد، بدون تغییر
۶ سناریوی wallet-intake روی هر دو دیتابیس
next build بدون خطا، TypeScript strict
قاعدهٔ جدید
هر تغییر در لایهٔ دیتابیس باید روی هر دو گویش آزموده شود.
محیط تست: Postgres روی ۵۴۳۳، MariaDB روی ۳۳۰۷.

۸. کارهای انجام‌نشده / گام‌های بعدی
 لایهٔ ابری + رمز عبور — کد آماده و تست‌شده است
 کلیدهای Supabase وارد شدند (توسط دستیار، در خود فایل)
 اجرای fix-app-settings.sql — انجام شد
 ساخت کاربر مدیر — انجام شد
 استقرار روی Vercel — انجام شد (hesab-app-nine.vercel.app)
 اجرای docs/fix-jdate.sql ← گام فعلی
 نقش «فقط مشاهده» برای شرکا (الان هر کاربر واردشده دسترسی کامل دارد)
 احتمالاً: نمودار ارزش ریالی در زمان، گزارش مقایسه‌ای، فیلتر بازه در داشبورد
 احتمالاً: همگام‌سازی زنده (Supabase Realtime) — الان باید رفرش کرد
سه سؤالی که هنوز از کاربر پرسیده نشده
برداشت بیش از موجودی وزنی: مسدود شود یا فقط هشدار؟ (الان: اجازه می‌دهد، وزن منفی ممکن است)
حذف تراکنش: الان حذف کامل است. در طرح اولیه «حذف نرم» پیشنهاد شده بود ولی پیاده نشد.
آیا تقویم‌نمای گرافیکی شمسی لازم است؟ (الان فقط تایپ عددی)
۹. نکات کار با این کاربر
فارسی صحبت می‌کند و پاسخ فارسی می‌خواهد.
فنی نیست — اصطلاحات را ساده توضیح دهید، گام‌به‌گام.
بازخوردهایش کوتاه و دقیق است؛ چند ایراد ریز را یک‌جا می‌گوید.
قبل از کد زدن، درک خود را بازگو کنید — در این پروژه چند بار جلوی کج‌فهمی را گرفت.
خروجی را با داده‌های واقعی تست می‌کند، پس دقت محاسبات حیاتی است.
۱۰. روش تست (بدون مرورگر)
Bash

# بررسی نحوی
node -e "const h=require('fs').readFileSync('public/index.html','utf8');
new Function(h.match(/<script>([\s\S]*)<\/script>/)[1]);console.log('OK')"
برای تست منطق، با Playwright فایل را باز کنید، partners و txs را مستقیم مقداردهی کنید،
render() یا buildReport() را صدا بزنید و innerText را بررسی کنید.
برای تست PDF: emulate_media(media="print") سپس pg.pdf(...)، و متن استخراج‌شده را
با unicodedata.normalize("NFKC", …) نرمال کنید (وگرنه حروف فارسی به شکل presentation form درمی‌آیند و مقایسه شکست می‌خورد).

۱۱. اتصال کیف پول ← سرمایه (تکمیل‌شده، شهریور ۱۴۰۴)
وضعیت: کار می‌کند ✅
تراکنش‌های کیف پول که یک طرفشان «سرمایه سلطانی» یا «سرمایه مزینانی» باشد،
خودکار در نرم‌افزار سرمایه با وضعیت pending ساخته می‌شوند.

معماری
دو مخزن جدا، دو پروژهٔ Vercel جدا:

مخزن	نقش	فایل‌های کلیدی
AmirMazinani561/hesab-app (خصوصی)	گیرنده	
route.ts
, 
wallet-map.ts
AmirMazinani561/Arena_Wallet (عمومی)	فرستنده	src/lib/equity-sync.ts, 
route.ts
آدرس سرمایه: https://hesab-app-zje1.vercel.app

متغیرهای محیطی
در Vercel کیف پول (نه سرمایه):

text

EQUITY_URL=https://hesab-app-zje1.vercel.app     # بدون اسلش آخر
EQUITY_TOKEN=<همان WALLET_INTAKE_TOKEN سرمایه>
در Vercel سرمایه:

text

WALLET_INTAKE_TOKEN=<توکن مشترک>
# WALLET_PARTNER_NAMES و WALLET_PARTNER_MAP لازم نیستند — تطبیق خودکار است
تطبیق خودکار نام (
wallet-map.ts
)
نام حساب کیف پول با نام شریک در دیتابیس جور می‌شود. نیازی به نگاشت دستی نیست.

normalizeFa() — نیم‌فاصله، ی/ک عربی، انواع الف، ة، اعراب، فاصلهٔ تکراری
coreName() — حذف کلمات عمومی: سرمایه، حساب، آقای، خانم، مهندس، دکتر، …
autoMatch() — سه مرحله: برابری کامل ← برابری هسته ← دربرگیری هسته
محافظه‌کار: اگر نام به بیش از یک شریک بخورد، null برمی‌گرداند (جلوگیری از واریز اشتباه)
تست‌شده: «سرمایه سلطانی»، «آقای سلطانی»، «سرمایه گذاری مزینانی»، «سرمايه سلطاني» (ی عربی) ← همه درست.

قواعد تصمیم
حالت	نتیجه
پول به حساب سرمایه	kind: IN (آورده)
پول از حساب سرمایه	kind: OUT (برداشت)
هیچ طرفی شریک نیست	skipped: no-partner
هر دو طرف شریک‌اند	skipped: internal-transfer
source_id تکراری	skipped: duplicate
تراکنش‌های واردشده status='pending' و price_rial_per_kg=null دارند ⇐
در وزن، درصد سهم، داشبورد و گزارش شرکت نمی‌کنند تا قیمت دستی وارد شود.

عیب‌یابی از راه دور
text

GET /api/health                      # بدون توکن
GET /api/wallet-intake               # با هدر x-intake-token
GET /api/wallet-intake?name=سرمایه سلطانی    # تست تطبیق یک نام خاص
/api/health برمی‌گرداند: build, columns, missingColumns, notNullBlockers.
فیلد build مهم‌ترین ابزار است — با آن می‌فهمید کد جدید واقعاً دیپلوی شده یا نه.
نسخهٔ فعلی: v5-nullable.

۱۲. باگ‌های این مرحله و درس‌ها
باگ: مهاجرت دیتابیس انجام نمی‌شد
create table if not exists روی جدول موجود هیچ کاری نمی‌کند. دیتابیس تولیدی
چند نسخه عقب مانده بود. سه خطای پشت‌سرهم که هرکدام بعدی را پنهان کرده بود:

column "source" does not exist
column "status" does not exist
null value in "price_rial_per_kg" violates not-null constraint
راه‌حل (در ensureDatabase()):

addColumnIfMissing() — با alter table … add column if not exists، بدون اتکا به
information_schema (روی pooler سوپابیس قابل اعتماد نیست)
dropNotNull() — برداشتن NOT NULL از ستون‌های اختیاری
backfill() — پرکردن پیش‌فرض امن برای ردیف‌های قدیمی
درس: ستون‌ها را دانه‌دانه وصله نکنید. کل ساختار موردنیاز را یک‌جا بررسی کنید.

باگ: فایل‌های نمونه بیلد را می‌شکستند
wallet-patch/ حاوی کد کیف پول بود ولی داخل مخزن سرمایه. next build
سعی می‌کرد تایپ‌چکشان کند و ۱۲ خطا می‌داد.
راه‌حل: پسوند .ts.txt + افزودن wallet-patch به exclude در 
tsconfig.json
.

دام: فایل‌ها در مخزن اشتباه
کاربر فایل‌های کیف پول را در مخزن سرمایه کامیت کرد. چند دور تست هدر رفت.
درس: با raw.githubusercontent.com مستقیم چک کنید فایل کجا نشسته
(مخزن کیف پول عمومی است). قبل از عیب‌یابی، وجود فایل را تأیید کنید.

دام: کد دیپلوی‌نشده
چند بار کد روی گیت‌هاب بود ولی Vercel نسخهٔ قدیمی سرو می‌کرد.
درس: همیشه یک ثابت نسخه (مثل BUILD) در پاسخ API بگذارید. بدون آن
نمی‌توان از بیرون فهمید کدام کد زنده است. x-vercel-cache: MISS یعنی
مشکل از کش نیست.

دام: کمبود حافظهٔ کارگاه
next build با Killed شکست می‌خورد وقتی چند سرور تست بالا بود.
راه‌حل: pkill -9 -f "next start" قبل از بیلد، و
NODE_OPTIONS="--max-old-space-size=1024".

نکته: شکست بی‌صدا در syncToEquity
اگر EQUITY_URL یا EQUITY_TOKEN تنظیم نباشد، تابع بی‌صدا return می‌کند.
این عمدی است (کیف پول نباید خراب شود) ولی عیب‌یابی را سخت می‌کند.
برای تشخیص: Runtime Logs کیف پول را ببینید — اگر هیچ پیام [equity-sync]
نبود یعنی متغیرها نرسیده‌اند. متغیر جدید بدون Redeploy اعمال نمی‌شود.

۱۳. گزارش، ورود گروهی و تبدیل واحد (شهریور ۱۴۰۴)
باگ: بخش گزارش اصلاً کد نداشت
رابط کاربری در body.ts کامل بود (r_p, r_d1, r_d2, r_go, r_out) ولی
تابع buildReport هنگام بازنویسی به Next.js جا مانده بود — دکمه به هیچ
چیز وصل نبود و سکوت می‌کرد.

روش تشخیص: grep -no "r_[a-z0-9]*" روی app.js و body.ts و مقایسهٔ
دو فهرست. هر شناسه‌ای که در HTML هست ولی در JS نیست، یعنی کد جاافتاده.
این تکنیک برای پیدا کردن بقیهٔ بخش‌های ناقص هم به کار می‌آید.

محتوای گزارش: گردش وزنی (مانده ابتدا/ورودی/خروجی/مانده پایان)،
گردش ریالی، سهم از کل، جدول ریز تراکنش‌ها با مانده تجمعی.
تراکنش‌های pending شمرده می‌شوند ولی در محاسبه نمی‌آیند.

باگ: کشوی سرمایه‌گذار گاهی خالی بود
fillSel() هنگام تغییر تب صدا زده نمی‌شد. اگر داده‌ها بعد از نخستین
رسم از سرور می‌رسیدند، کشو خالی می‌ماند.
راه‌حل: افزودن fillSel() به رویداد کلیک nav button + محافظ
if(!$('#n_p')||!$('#r_p')) return; برای زمانی که DOM هنوز آماده نیست.

ورود گروهی: ستون قیمت اختیاری شد
قبلاً if(!pr) errs.push('قیمت صفر/نامعتبر') ردیف‌های بدون قیمت را رد می‌کرد.
حالا ردیف بدون قیمت با s:'pending' و pr:null وارد می‌شود و بعداً از تب
«در انتظار قیمت» قیمت می‌گیرد.

ورود گروهی: پشتیبانی از بدهکار/بستانکار جدا
باگ خطرناک بود: فایل کاربر دو ستون جدا داشت
(بدهکار (برداشت) و بستانکار (واریز)). الگوی amount فقط به «بستانکار»
می‌خورد و ستون «بدهکار» به هیچ نقشی نمی‌خورد ⇐ همهٔ برداشت‌ها بی‌صدا گم
می‌شدند و محاسبهٔ سهم غلط درمی‌آمد.

راه‌حل: دو الگوی جدید debit و credit که پیش از amount بررسی
می‌شوند، دو کشوی جدید در رابط، و منطق: بستانکار پر ⇐ IN، بدهکار پر ⇐ OUT.
اگر هر دو خالی باشند ردیف خطا می‌خورد (جلوگیری از ورود ردیف «جمع کل»).

درس: هر وقت کاربر فایلی می‌آورد، عنوان دقیق ستون‌ها را بپرسید و
تشخیص خودکار را با همان‌ها تست کنید. حدس زدن ساختار خطرناک است.

ابزار تبدیل تومان به ریال (یک‌بار مصرف)
کارت جدید در بخش «داده و پشتیبان». تابع convertToRial() در app.js.

محافظ: کاربر باید کلمهٔ «تبدیل» را تایپ کند
قبل از اجرا خودکار فایل پشتیبان JSON دانلود می‌کند
t.a و t.pr هر دو در ۱۰ ضرب می‌شوند ⇐ وزن و درصد سهم تغییر نمی‌کند
curPrice هم ۱۰ برابر می‌شود تا هم‌واحد بماند
همگام‌سازی با سرور دانه‌دانه با cTxUpd، شمارش موفق/ناموفق
تأیید ریاضی: (a×10)/(pr×10) = a/pr. تست شد: وزن کل قبل و بعد ۱۴۰۰ kg.

۱۴. نکات محیط کار (برای مدل بعدی)
/tmp پایدار نیست: در این نشست چند بار وسط کار پاک شد و حتی
node_modules و بستهٔ postgres از بین رفت. برای تست، مسیرهای زیر
/home/user بسازید ولی حتماً آخر کار پاک کنید.
دیتادیر Postgres حدود ۴۶MB است. اگر داخل /home/user بسازید،
وارد اسنپ‌شات ورک‌اسپیس می‌شود و کاربر متوجه بزرگ شدن پروژه می‌شود.
الگوی درست: ساخت، تست، سپس pkill -9 -f postgres && rm -rf <datadir>.
حافظهٔ کارگاه ~۲GB است. next build با Killed شکست می‌خورد اگر
چند سرور تست بالا باشد. قبل از بیلد: pkill -9 -f "next start" و
در صورت نیاز NODE_OPTIONS="--max-old-space-size=1024".
npx tsc اشتباه است — بستهٔ بی‌ربط tsc@2.0.4 را نصب می‌کند.
درست: ./node_modules/.bin/tsc --noEmit.
برای تست سریع منطق JS بدون مرورگر: توابع را با regex از app.js
بیرون بکشید و در یک فایل موقت require کنید (eval مستقیم در
node -e به‌خاطر نقل‌قول‌های فارسی می‌شکند).
